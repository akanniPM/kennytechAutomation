{
  "name": "fixflow-core-backend",
  "version": "1.0.0",
  "description": "Vercel Serverless Backend for FixFlow Core Repair Hub Automation",
  "main": "api/intake.js",
  "scripts": {
    "dev": "vercel dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "vercel": "^34.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
